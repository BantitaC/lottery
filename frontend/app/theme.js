import { createTheme } from '@mui/material/styles';

const theme = createTheme({
    palette: {
        primary: {
            main: '#d12530',
            background: '#fff',
            bgGray: '#f0f0f0',
            fontMain: '#000',
            fontGray: '#464646',
            secondary: '#fdfae3',
            dropdown: '#ebe4ae',
        }
    },
});

export default theme;